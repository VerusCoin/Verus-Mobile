import React, { useState } from 'react';
import { SafeAreaView, Text } from 'react-native';

const Home = () => {
    return (
        <SafeAreaView style={{flex:1, marginTop:100, marginLeft:30}}>
            <Text style={{fontSize:20}}>HOME</Text>
        </SafeAreaView>
    );
}
export default Home;