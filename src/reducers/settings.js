export const ledger = (state = {
  btcFeesAdvanced: false,
  extendedCoinInfo: false,
  extendedTxInfo: false,
  pinForTxs: false,
}, action) => {
  switch (action.type) {
    default:
      return state;
  }
}